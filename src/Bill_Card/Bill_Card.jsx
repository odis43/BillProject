import React from "react";
import Popup from "reactjs-popup";

const Bill_Card = ({ name }) => {
    return(
        <div>
            <button>{name}</button>
        </div>
    );
}
export default Bill_Card


